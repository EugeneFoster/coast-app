/** Three stacked rule lines — brand divider motif */
export function HairlineMotif({ className = "" }: { className?: string }) {
  return <div className={`rule3 bg-bone ${className}`} aria-hidden />;
}
